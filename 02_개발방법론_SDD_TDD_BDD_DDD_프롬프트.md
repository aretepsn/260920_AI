# 프롬프트 — 02_개발방법론_SDD_TDD_BDD_DDD.html 재현용

다른 세션에 아래 [프롬프트 본문]을 그대로 붙여넣으면 동일한 산출물을 얻을 수 있다.

## 전제 조건

- 작업폴더 `claude`를 연결한 상태에서 실행
- 입력 파일 없음 (내용은 프롬프트 본문에 포함)
- 사용 스킬: 없음 (단일 HTML 파일 직접 작성)

## 프롬프트 본문

```
별도의 html 파일로 만들어서 아래 내용을 표현해줘.
일반 PC 화면에서 보는 용도로 구성할 것. [자동판단]

[표현할 내용 — 문구 그대로 사용]
SDD(Spec-Driven Development, 명세 기반 개발)
ㄴ개발 명세서(Spec)를 먼저 작성하고, AI가 그 명세를 기준으로 코드를 생성하게 만드는 개발 방식
TDD (Test-Driven Development, 테스트 주도 개발)
ㄴ코드를 작성하기 전에 실패하는 테스트 코드를 먼저 작성하고, 이를 통과하는 최소한의 코드를 구현하는 방식
BDD (Behavior-Driven Development, 행위 주도 개발)
ㄴ"사용자가 버튼을 클릭하면 ~게 동작해야 한다"처럼 사용자의 행위와 시나리오(Given-When-Then)를 중심으로 명세를 작성하고 개발하는 방식
DDD (Domain-Driven Development, 도메인 주도 개발)
ㄴ비즈니스 핵심 영역(도메인)의 용어와 모델을 정의하고, 이를 중심으로 시스템을 설계/개발하는 방식

[구성] [자동판단]
1. 페이지 제목: "개발 방법론 정리"
2. 2×2 카드 그리드 (순서: SDD, TDD, BDD, DDD)
   - 카드 상단: 약어 배지(고정폭 글꼴, 크게) + 영문 이름(작게) + 한글 이름(굵게)
   - 그 아래: "ㄴ" 관계를 꺾인 연결선(└ 모양)으로 표현하고 설명 문장 표시
   - 카드마다 상단 테두리·배지 색을 다르게 (주황, 초록, 파랑, 보라)

[출력 규칙]
- 설명 문구는 위 내용을 한 글자도 바꾸지 않고 사용. 내용 추가 없음 [자동판단]
- 최대 폭 1200px, 860px 미만에서는 1열로 전환 [자동판단]
- 라이트/다크 모드 지원, 맑은 고딕 우선, 외부 라이브러리·폰트 없는 단일 HTML [자동판단]
- 파일 형식: html
- 파일명: 02_개발방법론_SDD_TDD_BDD_DDD.html [자동판단]
- 저장 위치: 이 프롬프트 파일과 같은 폴더 (작업폴더 루트)
```

> `[자동판단]`이 붙은 줄은 원 요청자가 지시한 것이 아니라 재현을 위해 채워 넣은 조건이다.
> 바꿔도 무방하다. 표식이 없는 줄이 실제 요구사항이다.

## 재현 시 참고사항

- 원 요청일: 2026-09-17 / 요청자: 김경탁
- 생성 파일: `02_개발방법론_SDD_TDD_BDD_DDD.html`, `02_개발방법론_SDD_TDD_BDD_DDD_프롬프트.md`
- DDD는 일반적으로 "Domain-Driven Design"으로 불리지만, 요청 문구대로 "Domain-Driven Development"로 표기함.
