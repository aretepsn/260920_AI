# 프롬프트 — 클로드_프로젝트_폴더구조.html 재현용

다른 세션에 아래 [프롬프트 본문]을 그대로 붙여넣으면 동일한 산출물을 얻을 수 있다.

## 전제 조건

- 작업폴더 `claude`를 연결한 상태에서 실행
- 입력 파일 없음 (Claude Code 공식 문서 내용을 기반으로 작성)
- 사용 스킬: 없음 (단일 HTML 파일 직접 작성)

## 프롬프트 본문

```
Claude Code를 쓰는 개발자가, 사용자 전체 설정이 아니라 "개별 프로젝트 하나"에만 영향을 주는
Claude 관련 폴더·파일 구조를 모바일에서 보고 이해할 수 있는 HTML 페이지를 만들어줘.
프로젝트마다 특성이 달라서 프로젝트 단위로 무엇을 구성할 수 있는지 파악하는 게 목적이다.

[배경]
- 사용자 전체(~/.claude) 구조가 아니라 프로젝트 폴더 안의 구조만 다룬다.
- 처음엔 모바일용(탭 → 하단 팝업)으로 만들었다가, 일반 PC에서 보는 화면으로 재구성함.

[요청]
'aaa'라는 폴더에 프로젝트 하나가 있다고 가정하고, Claude 관련 폴더 구조를 트리로 보여줘.
폴더/파일을 클릭하면 그 항목의 설명을 보여줘.
폴더를 접었다 펼 수 있도록 해줘.
모바일이 아니라 일반 PC에서 볼 수 있도록 화면을 구성해줘.
PC에서는 팝업 대신 왼쪽 트리 + 오른쪽 상세 패널 2단 구성으로 설명을 보여줄 것. [자동판단]
작성 전에 Claude Code 공식 문서(code.claude.com/docs)로 각 파일의 현재 지원 여부를 확인할 것. [자동판단]

[구성]
1. 헤더: 제목 "한 프로젝트 폴더 안의 Claude 구조", aaa 폴더에서 실행 시 이 프로젝트에만 적용된다는 안내 [자동판단]
2. 범례 배지(헤더 오른쪽 박스): 공유·git 커밋 / 개인·git 제외 / 참고·지침 / 강제·설정 [자동판단]
3. 왼쪽 패널 트리 (각 행에 이름 + 한 줄 설명 + 번호) (번호 ①~⑪ 표시) [자동판단]
   aaa/
   ├ CLAUDE.md (1) / CLAUDE.local.md (2) / .mcp.json (3)
   ├ src/api/CLAUDE.md (4, 하위 폴더 전용 지침 예시)
   └ .claude/
     ├ settings.json (5) / settings.local.json (6)
     ├ rules/ (7) └ sql.md (paths 지정 예시)
     ├ skills/ (8) └ deploy/SKILL.md
     ├ agents/ (9) └ test-verifier.md
     ├ workflows/ (10) / output-styles/ (11)
     └ commands/ (예전 방식, skills 권장)
   오른쪽 패널: 선택 항목의 경로·이름·배지·한 줄 설명 + 섹션별 설명(왼쪽 제목 열 / 오른쪽 내용 열), 폴더는 "하위 항목" 버튼 목록(클릭 시 해당 항목으로 이동) [자동판단]
4. 왼쪽 패널 하단 "폴더 밖에 있지만 이 프로젝트 전용" 섹션: ~/.claude/projects/<프로젝트>/memory/ (자동 메모리) [자동판단]
5. 2단 레이아웃 아래 "헷갈리기 쉬운 구분" 카드 3개 (가로 3열): CLAUDE.md·rules·skills 차이 / 지침 vs 설정 / 공유 vs 개인 [자동판단]
6. 푸터: 기준 문서 링크(.claude 디렉토리, 메모리, 설정, Hooks), Cowork 앱은 별도라는 안내 [자동판단]

[상세 패널 내용 — 항목마다] [자동판단]
- 경로, 이름, 배지
- 기능 / 이런 내용을 넣어요(또는 이런 곳에 써요) / 예시 코드 / 만드는 법 / 주의 중 해당하는 것
- 반드시 반영할 사실:
  - CLAUDE.md: 매 세션 자동 로드, /init으로 초안 생성, .claude/CLAUDE.md 위치도 가능, 길면 묻히니 긴 절차는 rules·skills로
  - CLAUDE.local.md: 개인용, .gitignore에 추가
  - .mcp.json: claude mcp add --scope project로 생성 가능, 비밀값은 ${환경변수}로 참조
  - settings.json: permissions allow/deny, hooks(Stop hook로 테스트 강제 예시), env. hook 설정은 이 파일에 쓰며 .claude/hooks/는 공식 폴더가 아님
  - settings.local.json: "항상 허용" 누르면 쌓임, git 자동 제외, 설정 우선순위
  - rules: frontmatter paths로 경로 한정 적용, paths 없으면 항상 로드
  - skills: 폴더당 SKILL.md, /이름 호출 또는 description 기반 자동 사용, 필요할 때만 본문 로드
  - agents: 역할·tools·model 지정, /agents 명령으로 생성, 작업자와 검증자 분리 예시
  - commands: 예전 방식, skills로 옮기는 방법
  - 자동 메모리: MEMORY.md 목차 + 주제 파일, Claude가 기록, 내 PC에만 있음, /memory로 확인

[출력 규칙]
- 한국어, 해요체 [자동판단]
- PC 기준: 최대 폭 1280px, 1280×720 / 1440×900에서 가로 스크롤 없음 [자동판단]
- 레이아웃: 왼쪽 트리 패널 380~440px, 오른쪽 상세 패널은 화면 높이에 맞춰 고정(sticky)되고 내부 스크롤 [자동판단]
- 처음 열면 aaa/ 루트가 선택된 상태, 선택 항목은 트리에서 강조 표시 [자동판단]
- 키보드 탐색: ↑↓ 이동(이동 시 바로 상세 표시), ← 접기/상위로, → 펼치기/하위로, ESC 닫기 [자동판단]
- 창 폭 1024px 미만에서는 1단으로 바뀌고 설명은 가운데 모달로 표시 [자동판단]
- 접기/펼치기: 폴더 왼쪽 화살표 버튼으로 토글, 이름 클릭은 상세 표시 (두 동작 분리) [자동판단]
- 처음엔 모두 펼친 상태, 트리 위에 "모두 펼치기 / 모두 접기" 버튼 (모두 접기 시 루트 aaa/는 펼친 채 유지, 선택 항목이 숨겨지면 보이는 상위 폴더로 선택 이동) [자동판단]
- 접힌 폴더는 이름 옆에 하위 항목 수 표시 (예: · 12개) [자동판단]
- 라이트/다크 모드 모두 지원 (prefers-color-scheme) [자동판단]
- 한글 글꼴은 맑은 고딕 우선, 외부 라이브러리·폰트 없이 단일 HTML 파일 (CSS/JS 인라인) [자동판단]
- 코드 예시는 textContent로 출력해 이스케이프 문제 방지 [자동판단]
- 파일 형식: html
- 파일명: 클로드_프로젝트_폴더구조.html [자동판단]
- 저장 위치: 이 프롬프트 파일과 같은 폴더 (작업폴더 루트)
```

> `[자동판단]`이 붙은 줄은 원 요청자가 지시한 것이 아니라 재현을 위해 채워 넣은 조건이다.
> 바꿔도 무방하다. 표식이 없는 줄이 실제 요구사항이다.

## 재현 시 참고사항

- 원 요청일: 2026-09-17 / 요청자: 김경탁
- 생성 파일: `클로드_프로젝트_폴더구조.html`, `클로드_프로젝트_폴더구조_프롬프트.md`
- Claude Code의 파일 구조·기능은 버전에 따라 바뀔 수 있음. 재실행 시 공식 문서 기준으로 항목이 달라질 수 있음.
- 예시 코드(gradle 명령, 스킬·에이전트 내용)는 설명용 가상의 예시이며 실행 때마다 표현이 달라질 수 있음.
